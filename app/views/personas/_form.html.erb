<%= form_for [@arcana, @persona] do |f| %>
  <% if @persona.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@persona.errors.count, "error") %> prohibited this persona from being saved:</h2>

      <ul>
      <% @persona.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :base_level %><br>
    <%= f.text_field :base_level %>
  </div>
  <div class="field">
    <%= f.label :max %><br>
    <%= f.check_box :base_level %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
